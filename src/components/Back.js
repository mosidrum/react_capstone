import React from 'react';
import { NavLink } from 'react-router-dom';

const Back = () => (
  <NavLink to="/">
    <button
      type="submit"
    >
      Back
    </button>
  </NavLink>
);

export default Back;
