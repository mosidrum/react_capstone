const category = [];
const data = [
"Asia","Europe","Africa","Oceania","Americas","Polar","Antarctic Ocean","Antarctic"
];

data.forEach((each) => {
  category.push(each);
});

export default category;
